# TSA_Aggregator_Compose
Docker runner of the GUI

To run webapp 
1. clone this repo with submodule:\
   `git clone --recurse-submodules https://github.com/tsa-korpus-parsing/TSA_Aggregator_Compose`
3. run `docker compose up --build`
